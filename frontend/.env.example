# Laravel backend API base URL (no trailing slash).
# Docker: http://localhost:8201/api
# Local backend: http://localhost:8000/api (if you run `php artisan serve`)
NEXT_PUBLIC_API_URL=http://localhost:8201/api
